import { Modal } from 'bootstrap';

require('./bootstrap');

window.novaUrlModal = new Modal(document.getElementById("novaUrl"));
window.editarUrlModal = new Modal(document.getElementById("editarUrl"));
